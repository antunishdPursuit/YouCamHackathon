{
  "name": "@yincol/shared",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Framework-free domain types and the deterministic palette engine. No network, ever.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./palette": "./src/palette/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
